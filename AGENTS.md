
# pokemon-SnapCrop — AGENTS.md

## プロジェクト名: **pokemon-SnapCrop**

## プロジェクトの目的

このリポジトリでは、**Windows / Edge / Chrome** を主な利用環境とし、  
**GitHub Pages で公開できる静的Webアプリ** を開発する。

このアプリの目的は、対戦画面をブラウザ上に表示しつつ、

- 右側の**敵パーティ表示領域だけを縦長にクロップ表示**
- 画面下の余白に**IDEのターミナル風CLIパネル**を配置
- CLI にポケモン名を入力すると、同梱CSVから該当行を引いて
  - タイプ
  - 特性
  - 種族値（`H, A, B, C, D, S`）
  を表示

できるツールを作ること。

これは **作業用ユーティリティ** であり、見た目よりも  
**対戦中に素早く確認できること** を優先する。

---

## 最重要方針

- まず repo を確認し、既存構成・制約・関連ファイルを把握してから作業する
- 複数ファイルにまたがる変更では、**いきなり実装せず先に計画を立てる**
- 完璧主義で止まらず、まず **MVP を成立** させる
- ブラウザ仕様や GitHub Pages 制約に当たった場合は、誤魔化さず明示する
- 不要な機能追加はしない
- 同じミスを繰り返したら、再発防止ルールをこの `AGENTS.md` に追記する

---

## スコープ

### MVP に含めるもの
- キャプチャデバイスまたは OBS 仮想カメラの映像表示
- 敵パーティ領域の **1クロップのみ**
- クロップ領域の手動調整
- クロップ結果の常時表示または更新表示
- 下部 CLI 風パネル
- ポケモン名入力による CSV 検索
- 該当ポケモンの基本情報表示
- GitHub Pages 公開
- PWA 対応
- GitHub Actions による自動 deploy

### MVP では無理に入れないもの
- 自動認識
- OCR
- 複雑なコマンド体系
- 自分側パーティのクロップ
- 高度な検索構文
- サーバー保存
- 外部API依存

---

## 基本制約

### アーキテクチャ
- **静的サイト**として実装する
- バックエンドは使わない
- 明確な理由がない限り、外部ライブラリは使わない
- 明確な理由がない限り、ビルド工程なしで動く構成を優先する
- GitHub Pages にそのまま載せられる構成にする

### 推奨ファイル構成
必要なら 1 ファイルにこだわらず、以下のような小規模構成でよい。

- `index.html`
- `style.css`
- `app.js`
- `data/` もしくは `assets/`
  - ポケモンCSV
- `manifest.webmanifest`
- `sw.js`
- `icons/`
- `.github/workflows/deploy.yml`
- `README.md`

### パス
- ルート相対パスを避ける
- 相対パスで動くようにする
- repo名付き GitHub Pages URL 配下でも壊れにくくする

### 言語
- UI: 日本語
- README: 日本語
- エラー表示: 日本語
- 進捗/状態表示: 日本語
- コミットメッセージ: 英語

---

## UI の全体構成

画面は大きく **上下2段** にする。

### 上段
- 左: ゲーム画面のライブ表示
- 右: 敵パーティ領域の縦長クロップ表示

### 下段
- CLI / ターミナル風パネル
- 入力欄
- 実行結果の表示欄

### レイアウトの意図
- 右クロップは、ゲーム画面の横に縦長で置く
- 余る下方向のスペースを、CLI パネルとして活用する
- 画面全体を、横長に無理やり広げるのではなく、**上段=映像 / 下段=情報参照** に整理する

---

## 映像まわりの要件

### 必須
- `MediaDevices` API を使う
- `enumerateDevices()` で映像入力一覧を取得する
- ユーザーが映像入力デバイスを選択できるようにする
- `getUserMedia()` で選択デバイスの映像を取得する
- `video` 要素に表示する
- キャプボ直入力と OBS 仮想カメラの両方を想定する

### 注意
- `file://` 直開き前提にしない
- localhost または GitHub Pages 前提で使う
- 権限取得前と後で UI 状態を明確に分ける
- 映像が未取得の状態では、クロップ更新に依存する操作を無効化または制限する

---

## クロップまわりの要件

### 対象
- **敵パーティ表示領域のみ** をクロップ対象とする
- クロップ領域は 1 つだけ

### 機能
- クロップ矩形をライブ映像上に重ねて表示する
- ドラッグ移動できる
- リサイズできる
- 必要なら `x / y / width / height` を数値入力でも編集できる
- ライブ映像表示サイズと元映像サイズの座標変換を正しく扱う
- 実際の `videoWidth / videoHeight` を使う
- 1080p 前提で雑に固定しない

### 保存
- クロップ設定は `localStorage` に保存する
- 再読み込み時に復元する
- 映像サイズに対して不正な値なら安全に補正する

### 表示
- 右側には、切り出し済みの敵パーティ領域を見やすく表示する
- 「クロップ結果を撮影して静止表示」でもよいし、「現在フレームに追従して更新」でもよい  
  ただし、MVP として無理のない実装を優先する

---

## CLI パネルの要件

### 位置と見た目
- 画面下部に配置する
- IDE のターミナル風の見た目を意識する
- ただし本物のシェルではなく、**CLI風の検索UI** として実装する
- 見た目だけに凝りすぎない
- 実用性を優先する

### MVP の入力仕様
- 入力は **ポケモン名のみ**
- コマンド体系は不要
- Enter または検索ボタンで実行できれば十分

### MVP の出力仕様
入力されたポケモン名に対応する CSV の行を検索し、少なくとも以下を表示する：

- タイプ
- 特性
- 種族値 `H / A / B / C / D / S`

### 表示形式
以下のような、CLI風の簡潔な出力でよい：

```text
> ガブリアス
タイプ: ドラゴン / じめん
特性: すながくれ / さめはだ
種族値: H108 A130 B95 C80 D85 S102
````

### エラー時

* 該当ポケモンが見つからない場合は、日本語で分かりやすく表示する
* CSV 読み込み失敗時も、日本語で明示する
* 入力空欄時は無意味に検索しない

---

## CSV データの扱い

### 前提

* ユーザーが用意した CSV を同梱データとして使う
* 外部APIは使わない
* MVP では **1行検索** ができればよい

### 期待するデータ

少なくとも以下の情報を扱えること：

* タイプ
* 特性
* `H, A, B, C, D, S`

### 実装ルール

* CSV の列名は実データに合わせて確認してから実装する
* 列名を決め打ちしすぎない
* 必要なら読み込み時に正規化する
* 検索キーはまず「完全一致優先」でよい
* 余計な曖昧検索やかな漢字変換補助は MVP では不要

### 注意

* CSV の実列名が想定と異なる場合は、勝手に捏造せず確認する
* MVP では「ポケモン名を入力すると、その行をそのまま読む」程度でよい
* 将来の拡張を邪魔しない程度に、データ取得部分は分離しておくとよい

---

## PWA 要件

* `manifest.webmanifest` を用意する
* `sw.js` を用意する
* インストール可能な最低限の構成にする
* キャッシュはアプリシェル中心でよい
* カメラ機能までオフラインで完全動作するような誤解を与えない
* PWA の目的は、**導線改善とアプリ風起動** である

---

## GitHub Actions 要件

* `main` への push で GitHub Pages に自動 deploy する
* できるだけ現行の標準的な Pages Actions 構成を使う
* workflow はシンプルで読みやすくする
* 静的ファイル構成に合わせて、余計なビルド手順を入れない

---

## デザイン方針

これは **作業用アプリ** であり、LPや広告ページではない。

### 基本方針

* 落ち着いた UI
* 情報密度は高すぎず低すぎず
* 余計なカードだらけにしない
* 主要領域は明確に分ける
* 目立たせるべきものは

  * 映像
  * 敵側クロップ
  * CLI入力
  * 検索結果
    に絞る

### 避けること

* 無意味なグラデーション装飾
* なんとなくダッシュボード風の細かいパネル乱立
* 角の過度な丸め
* 飾りのためのアイコン追加
* マーケ文言
* 操作と関係ない派手な動き

### よい方向

* 最小限の色数
* 最低限の余白
* 見出しより中身が主役
* 説明がなくても分かるシンプルなUI
* プレイ中に視線移動しやすい整理された配置

---

## エラー処理・状態表示

最低限、以下は実装すること：

* デバイス未接続
* 権限未付与
* 権限拒否
* ストリーム開始失敗
* CSV 読み込み失敗
* ポケモン名未検出
* 空入力
* 映像未準備

すべて日本語で、短く分かりやすく表示する。

---

## README 要件

`README.md` には少なくとも以下を書くこと：

* このツールで何ができるか
* 対応環境
* ローカル起動方法
* なぜ `file://` ではなくローカルサーバーが必要か
* 映像デバイスの選択方法
* クロップ領域の調整方法
* CLI パネルの使い方
* CSV の前提列
* `localStorage` に保存する内容
* PWA について
* GitHub Pages 公開方法
* GitHub Actions 自動 deploy の説明
* 既知の制約

---

## 検証方針

完了前に、可能な範囲で以下を確認すること：

1. ローカルでアプリが開く
2. GitHub Pages パス前提でも壊れない
3. 映像デバイス一覧が表示される
4. 映像開始ができる
5. クロップ領域が調整できる
6. 右側クロップ表示が機能する
7. CLI 入力が動く
8. CSV から該当行を読める
9. 名前・タイプ・特性・種族値を表示できる
10. `localStorage` が復元される
11. PWA 登録がアプリを壊していない
12. GitHub Actions workflow が妥当
13. README が現実に合っている

確認していないことを、確認済みと書かないこと。

---

## 完了条件

以下をすべて満たすまで完了扱いにしない：

* 静的サイトとして構成されている
* GitHub Pages 対応になっている
* 敵側クロップ1本の表示が成立している
* CLI 風検索パネルが動く
* ポケモン名入力で CSV の該当行を表示できる
* タイプ・特性・種族値 `H A B C D S` が表示できる
* `localStorage` 保存と復元がある
* 日本語エラー表示がある
* PWA ファイルがある
* GitHub Actions deploy workflow がある
* README が更新されている
* 最終差分を見直し、不要な複雑化を避けている

---

## やってはいけないこと

* バックエンドを追加しない
* 外部API依存にしない
* OCR を勝手に足さない
* 自動認識を勝手に足さない
* 2クロップ前提に戻さない
* 使わない装飾UIを増やさない
* CSV 仕様を勝手に捏造しない
* 実装していない機能を README に書かない

---

## 詰まったときの報告形式

詰まった場合は、以下の順で報告すること：

1. 何が blocker か
2. それがブラウザ制約 / デバイス制約 / 実装不備のどれか
3. 最善の代替策は何か
4. それでも前進できる最小の次手は何か